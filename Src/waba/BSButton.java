
import waba.fx.*;
import waba.ui.*;
import waba.util.*;
import waba.sys.*;
import superwaba.ext.xplat.ui.*;

public class BSButton extends Button
{
    public BSButton (Image img)
    {
        super (img);
        backColor = new Color (0xC0, 0xC0, 0xC0);
    }
    public BSButton (String text)
    {
        super (text);
        backColor = new Color (0xC0, 0xC0, 0xC0);
    }
}
